# Inwise Cloud

Use the Inwise MCP tools as the source of record for questions about meetings, decisions, people, projects, goals, action items, and briefings. Start with `whoami` when identity or workspace scope is uncertain. Retrieve only the evidence needed, preserve source dates and links, label inference, and never claim to modify Inwise because the hosted tools are read-only.

For detailed routing, load the `inwise-workspace-memory` skill when it is available.
